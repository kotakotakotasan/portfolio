# オンラインポートフォリオ / 武蔵大学 社会学部 メディア社会学専攻 メディア・クリエーション方式

## このプロジェクトについて

武蔵大学 社会学部 メディア社会学科 メディア社会学専攻の総合型選抜（メディア・クリエーション方式）に提出するオンラインポートフォリオ。制作者は映像制作者（登録者400万人規模のYouTubeチャンネルで企画・構成・演出・出演を担当、自身のチャンネルでも検証運用、学校での課題解決型プレゼン、地方PR映像、起業体験プログラム最優秀賞）。

**このサイトは作品集であると同時に、二次選考のプレゼン資料でもある。**

配点は以下の通り。ここから設計判断がすべて導かれる。

| 選考 | 配点 |
|---|---|
| 一次 | 自己紹介書 20% / 調査書・その他出願書類 20% / **オンラインポートフォリオの出来映え 60%** |
| 二次 | 小論文 40% / **オンラインポートフォリオのプレゼンテーション面接 60%** |

要項が明示する評価軸は「自己PR、クリエイティビティ、セルフプロデュース、Web表現などの能力」。

要項の警告文（原文の趣旨）:
> 本学科はメディア社会学を学ぶ場所なので、あまりにも抽象的な表現や説明不足な作品は、芸術的に優れていても評価は低くなる。広い意味でのメディア研究・社会学研究に通じる表現を期待する。

つまり **「かっこいいが何も説明していないポートフォリオ」は要項が名指しで低評価と言っている**。ビジュアル優先のBehance的な構成に寄せないこと。

---

## 絶対に破ってはいけない制約

### 1. 外部依存ゼロ（最重要）

要項は「二次選考日には大学が貸し出すWindows PCでWebにアクセスしてプレゼンできること」「接続できない場合に備え、インターネットに接続しなくてもプレゼンできる準備をすること」を要求している。

したがって:

- **CDNを一切使わない。** Tailwind CDN、Google Fonts、外部CSS/JS、外部アイコンフォント、すべて禁止
- フォント・CSS・JS・画像はすべてリポジトリ内に同梱する
- 参照はすべて相対パス。`file://` でindex.htmlを直接開いても完全に動くこと
- ビルドステップを作らない。素のHTML/CSS/JSで書く

この設計にすると、公開版のフォルダをそのままZIPにすればオフライン版になる。二重管理をしない。

**唯一の例外はYouTube埋め込み**。これはオフラインでは動かないので、後述のfacadeパターンで対処する。

### 2. パフォーマンス

要項は「この期間中にフリーズする、切り替えが遅いなどの不具合が生じないよう」と明記している。

- 画像は表示サイズに合わせてリサイズしてから配置する。元データをそのまま置かない
- 初期表示でiframeを読み込まない（facadeパターン）
- 重いアニメーション、パララックス、大量のDOM操作を作らない

### 3. 著作権（違反すると審査対象から除外される）

要項は「著作権もしくは商標の侵害その他の知的財産の不正利用、なりすまし、不法行為、ヘイトスピーチ、特定の人や組織を標的とした誹謗中傷や嫌がらせについては、それらに類する表現が見受けられた時点で審査対象から除外」と書いている。除外は不合格より重い。

- **他者チャンネルの動画は公式のYouTube埋め込みプレーヤーのみ。** サムネイル画像のスクリーンショット転載・再アップロードは禁止
- 参加した案件については **自分の役割を明記する**（例：「企画・構成・演出・出演として参加」）。自分のチャンネルと誤読される書き方をしない（なりすまし判定の回避）
- 新聞記事・大学サイト等の文言をそのまま貼らない。自分の言葉で書き直すか、出典を明記して最小限に留める
- 第三者（特に子ども）の顔が写る素材は、肖像の許諾が確認できたものだけ使う。不明なものは使わない
- 素材の権利状況が不明なときは、使わずに実装者に確認を返す。勝手に判断しない

### 4. 完成日時以降の改変禁止

作品概要書に記載した「完成した日時」以降に内容を改変すると **大幅な減点対象**。

したがって作業順序は固定:

```
全デバイスで動作確認 → デプロイ → 公開URLで再確認 → git tag で凍結 → 完成日時を確定 → 作品概要書に記入
```

この順を絶対に崩さない。日時を先に決めてから直す、をやらない。

---

## 役割分担

### Claude Code がやること

- HTML / CSS / JS の実装
- レイアウト、タイポグラフィ、レスポンシブ対応
- 画像の最適化、QRコード生成
- 動作検証、デプロイ

### Claude Code がやらないこと

**各作品の説明文・志望の軸となる文章は、本人が `content/content.md` に書く。Claudeはそれを整形・実装するだけで、代筆しない。**

理由は二次選考の配点60%が個別のプレゼン面接だから。書いた本人が説明できない文章がサイトに載っていると、面接で必ず崩れる。文言の推敲提案は歓迎するが、書き換えは本人の確認を取ってから。

---

## 各作品の記述構造

すべての作品セクションを、この4段で統一する。この構造自体が「メディア研究に通じる表現」の証明になる。

1. **狙い（仮説）** — 何を起こしたかったのか
2. **打ち手** — そのためにどの構成・演出を選んだか
3. **返ってきた反応** — 再生数、視聴維持率、コメントの中身（定量＋定性）
4. **得た知見** — 次に転用できる形に一般化すると何か

**4段目が最重要。** ここが「上手い映像」を「検証された知見」に変える。1〜3だけだと自慢に見える。

`content.md` に4段目が欠けているセクションがあれば、実装せずに本人に指摘を返すこと。

### 全体の構成方針

- 冒頭に **問い**（若者の可能性がなぜ狭まるのか / メディアはそれをどう動かせるのか）を短く置く。志望理由ではなく、作品群を貫く軸として
- 自分でYouTubeチャンネルを立ち上げて検証した経験は、**仮説→実験→知見→本業への還元** という完全なループになっている。これを実績の羅列に埋もれさせず、記憶に残る位置（最初か最後）に置く
- 「貴学では〜を学びたい」という直接的な志望動機の宣言は **最後の1画面で短く**。長く書かない。それは自己紹介書の仕事であり、ポートフォリオの評価軸（自己PR / クリエイティビティ / セルフプロデュース / Web表現）に乗らない

---

## ディレクトリ構成

```
portfolio/
├── CLAUDE.md
├── index.html
├── assets/
│   ├── css/
│   ├── js/
│   ├── fonts/          # サブセット化した woff2 を同梱
│   └── img/            # 最適化済み。元データは含めない
├── content/
│   └── content.md      # 本人が書く原稿。実装の入力
├── _source/            # 元画像・作業ファイル。公開物には含めない
└── dist/               # オフライン版ZIPの生成先
```

---

## 実装方針

### YouTube埋め込み（facadeパターン）

iframeを直接置かず、**静止画のプレースホルダをクリックしたときに初めてiframeを生成する**。

- 初期表示が軽くなり、要項の「フリーズしない」要件を満たす
- オフライン時もプレースホルダの静止画とテキストは表示される
- プレースホルダ画像はYouTubeのサムネイルではなく、**自作の構成図・コンテ・キーフレーム図解**を使う（著作権の観点でも、説明の観点でも優れている）

この設計判断は二次のプレゼンで説明できる材料になるので、なぜこうしたかをコメントに残しておく。

### フォント

日本語Webフォントは数MB級で重い。要項のパフォーマンス要件に直接抵触する。

選択肢は2つ。どちらでもよいが、決めた理由を残すこと。

- **A: システムフォント** — 追加読み込みゼロで最速。ただし **二次選考は大学貸出のWindows PCで表示される** ので、font-stackはWindows（Yu Gothic / Meiryo）で破綻しないこと。macOSだけで確認して終わらせない
- **B: サブセット化したwoff2を同梱** — 実際に使う文字だけに絞る。`fonts/` に置いて `@font-face` で相対パス参照。CDN参照は禁止

いずれの場合も、**Windows環境での表示確認を必ず行う**。

### CSS

- CSS変数でトークン（色・タイプスケール・余白）を定義してから書く
- セレクタの詳細度がぶつかって打ち消し合わないよう注意。特にセクション間のpadding/margin
- キーボードフォーカスを可視に保つ。`prefers-reduced-motion` を尊重する

---

## デザインの進め方

いきなりコードを書かない。2パスで進める。

**パス1: 設計案を出す**

- 色: 4〜6個の名前付きhex
- タイプ: 書体とその役割（1〜2ファミリー。2つなら明確に差をつける）
- レイアウト: 一文の説明とASCIIワイヤーフレーム。左揃え/中央揃えの方針も
- 原則: このページを固有のものにしている判断は何か

**パス2: 案をブリーフに照らして見直してから実装する**

「同じような依頼なら誰にでも出す答え」になっている部分があれば、そこを直してから書き始める。何を変えたか、なぜ変えたかを言葉にする。

### 避けるべき既定値

以下は生成物の典型的な特徴として認識されやすい。ブリーフが指定していない軸を、これらに使わない。

- クリーム地（#F4F1EA近辺）＋高コントラストのセリフ見出し＋テラコッタ系アクセント（#D97757近辺）の組み合わせ
- ほぼ黒の背景に、明るいアシッドグリーンや朱色のアクセント1色
- ヘアラインの罫線＋角丸ゼロ＋新聞のような密なカラム
- 内容を同じ角丸カードに刻んで並べ、全部に同じ薄いグレーの影を敷く構成
- 見出しの上に字間を開けた全角/半角の大文字ラベルを毎回置く
- 中黒で連結したメタ情報（'A · B · C'）
- リンクやボタンの文言末尾に '→' を付ける
- 見出しの中の1語だけを色や斜体で強調する

### この案件固有の手がかり

デザインの方向は、被写体から取る。この人が扱ってきたのは、未就学児〜小学校高学年に届く物語形式の映像であり、ハラハラ・ドキドキ・感動という感情の設計であり、その裏側にある「若者の可能性が狭まる」という重いテーマ。この落差そのものが素材になる。

大胆さは1箇所に集中させる。1つを記憶に残る要素にして、周りは静かに保つ。

---

## 検証チェックリスト

デプロイ前に全項目を通す。1つでも落ちたら公開しない。

- [ ] 375px / 768px / 1280px / 1920px で崩れない
- [ ] `file://` で index.html を直接開いて、レイアウト・フォント・画像が正常
- [ ] ネットワークを切った状態で開き、YouTube以外がすべて表示される
- [ ] **スマホ実機**で公開URLを開いて確認（エミュレータだけでは不可）
- [ ] **Windows環境**でフォントが破綻しない
- [ ] 外部ドメインへのリクエストがYouTube以外に存在しない（DevToolsのNetworkで確認）
- [ ] 全画像が最適化済み。ページ全体の初期読み込みが軽い
- [ ] キーボードのTab移動でフォーカスが見える
- [ ] 使用素材の権利が全点クリア（他者チャンネルのサムネ画像なし、無許諾の顔写真なし、無断転載の文言なし）
- [ ] 各作品セクションに4段（狙い/打ち手/反応/知見）が揃っている

---

## 凍結手順

チェックリスト通過後:

1. デプロイ（GitHub Pages推奨。コミット履歴が「完成日時以降、改変していない」ことの証明になる）
2. 公開URLでスマホ実機・Windowsを再確認
3. `git tag -a v1.0 -m "完成"` で凍結
4. タグの日時を作品概要書の「完成した日時」として記入
5. 公開URLのQRコードを生成（印刷解像度を指定できるようpythonで生成する。オンライン生成サイトは使わない）
6. **以降、内容を改変しない**

作品概要書（A4一枚、書式自由）に必要な3点:

1. オンラインポートフォリオのリンク先（URLおよびQRコード）
2. オンラインポートフォリオが完成した日時
3. オンラインポートフォリオの概要

閲覧可能期間は 2026年9月1日 〜 2026年10月11日。この期間中ずっとアクセスできる状態を維持する。

---

## 二次選考（10/11）に向けて

一次通過後の作業。今は着手しない。

- `dist/` にオフライン版ZIPを作成し、USBメモリに入れる
- Windows機で `file://` から開き、プレゼンが通しでできることを確認
- 大学貸出PCはWindows。ブラウザはChrome/Edgeを想定
